name: 部署到 GitHub Pages

on:
  push:
    branches: [ source ]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v4

    - name: Generate index.html
      run: |
        cat > index.html << 'EOF'
        <!DOCTYPE html>
        <html lang="zh-CN">
        <head>
          <meta charset="UTF-8">
          <meta name="viewport" content="width=device-width, initial-scale=1.0">
          <title>加密文件库</title>
          <script src="https://cdnjs.cloudflare.com/ajax/libs/crypto-js/4.1.1/crypto-js.min.js"></script>
          <style>
            *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }
            body {
              font-family: 'Segoe UI', system-ui, sans-serif;
              background: #0f0f1a;
              color: #e0e0e0;
              min-height: 100vh;
              display: flex;
              align-items: center;
              justify-content: center;
            }
            #login {
              background: #1a1a2e;
              border: 1px solid #2a2a4a;
              border-radius: 16px;
              padding: 2.5rem 2rem;
              width: 360px;
              text-align: center;
              box-shadow: 0 20px 60px rgba(0,0,0,0.5);
            }
            #login h1 { font-size: 1.6rem; margin-bottom: 0.4rem; }
            #login p  { color: #888; font-size: 0.9rem; margin-bottom: 1.8rem; }
            #login input {
              width: 100%; padding: 12px 14px;
              background: #0f0f1a; border: 1px solid #333;
              border-radius: 8px; color: #e0e0e0; font-size: 1rem;
              margin-bottom: 1rem; outline: none;
              transition: border-color .2s;
            }
            #login input:focus { border-color: #7c6af7; }
            #login button {
              width: 100%; padding: 12px;
              background: linear-gradient(135deg, #7c6af7, #5e9cf7);
              border: none; border-radius: 8px;
              color: #fff; font-size: 1rem; cursor: pointer;
              transition: opacity .2s;
            }
            #login button:hover { opacity: .85; }
            #login-err { color: #f77; font-size: .85rem; margin-top: .8rem; min-height: 1.2em; }
            #app {
              display: none;
              width: 100vw; height: 100vh;
              flex-direction: row;
            }
            #sidebar {
              width: 260px; min-width: 200px;
              background: #1a1a2e;
              border-right: 1px solid #2a2a4a;
              display: flex; flex-direction: column;
              overflow: hidden;
            }
            #sidebar-header {
              padding: 1.2rem 1rem;
              border-bottom: 1px solid #2a2a4a;
              font-weight: 600; font-size: .9rem;
              color: #aaa; letter-spacing: .05em;
            }
            #tree { flex: 1; overflow-y: auto; padding: .5rem 0; }
            #tree::-webkit-scrollbar { width: 4px; }
            #tree::-webkit-scrollbar-thumb { background: #333; border-radius: 2px; }
            .tree-item {
              display: flex; align-items: center; gap: 6px;
              padding: 6px 12px; cursor: pointer;
              font-size: .88rem; user-select: none;
              transition: background .15s;
              white-space: nowrap; overflow: hidden; text-overflow: ellipsis;
            }
            .tree-item:hover { background: #252540; }
            .tree-item.active { background: #2e2e55; color: #a89cf7; }
            .tree-item .icon { flex-shrink: 0; }
            .tree-item .label { overflow: hidden; text-overflow: ellipsis; }
            .tree-folder > .tree-children { display: none; }
            .tree-folder.open > .tree-children { display: block; }
            .tree-folder > .tree-item .arrow { font-size: .7rem; color: #666; transition: transform .2s; flex-shrink: 0; }
            .tree-folder.open > .tree-item .arrow { transform: rotate(90deg); }
            .tree-children { padding-left: 16px; }
            #main { flex: 1; display: flex; flex-direction: column; overflow: hidden; }
            #topbar {
              padding: .9rem 1.4rem;
              border-bottom: 1px solid #2a2a4a;
              font-size: .85rem; color: #666;
              background: #16162a;
            }
            #viewer { flex: 1; overflow: auto; padding: 1.5rem; }
            #viewer::-webkit-scrollbar { width: 6px; }
            #viewer::-webkit-scrollbar-thumb { background: #333; border-radius: 3px; }
            #placeholder {
              height: 100%; display: flex; flex-direction: column;
              align-items: center; justify-content: center;
              color: #444; gap: .6rem;
            }
            #placeholder .big { font-size: 3rem; }
            #file-content {
              display: none;
              background: #1a1a2e; border: 1px solid #2a2a4a;
              border-radius: 10px; padding: 1.4rem;
            }
            #file-content h2 { font-size: 1rem; color: #a89cf7; margin-bottom: 1rem; }
            #file-content pre {
              white-space: pre-wrap; word-break: break-word;
              font-family: 'Cascadia Code', 'Fira Code', monospace;
              font-size: .85rem; line-height: 1.6; color: #ccc;
            }
            #file-content iframe {
              width: 100%; height: 70vh; border: none;
              border-radius: 6px; background: #fff;
            }
            #loading { display: none; text-align: center; padding: 3rem; color: #666; }
            .md-body { line-height: 1.8; color: #ccc; }
            .md-body h1 { font-size: 1.5rem; color: #fff; margin: 1rem 0 .5rem; }
            .md-body h2 { font-size: 1.2rem; color: #e0e0e0; margin: 1rem 0 .4rem; }
            .md-body h3 { font-size: 1rem; color: #ccc; margin: .8rem 0 .3rem; }
            .md-body ul  { padding-left: 1.4rem; margin: .4rem 0; }
            .md-body blockquote { border-left: 3px solid #7c6af7; padding-left: .8rem; color: #888; margin: .5rem 0; }
            .md-body hr  { border: none; border-top: 1px solid #2a2a4a; margin: 1rem 0; }
            .md-body strong { color: #fff; }
          </style>
        </head>
        <body>
        <div id="login">
          <h1>🔐 加密文件库</h1>
          <p>请输入密码以访问内容</p>
          <input type="password" id="pwd" placeholder="访问密码" />
          <button onclick="doLogin()">进入</button>
          <div id="login-err"></div>
        </div>
        <div id="app">
          <div id="sidebar">
            <div id="sidebar-header">📁 文件目录</div>
            <div id="tree"></div>
          </div>
          <div id="main">
            <div id="topbar">选择左侧文件查看内容</div>
            <div id="viewer">
              <div id="placeholder"><div class="big">🗂️</div><div>从左侧选择一个文件</div></div>
              <div id="loading">解密中...</div>
              <div id="file-content"><h2 id="file-title"></h2><div id="file-body"></div></div>
            </div>
          </div>
        </div>
        <script>
          let PASSWORD = '';
          document.getElementById('pwd').addEventListener('keydown', e => { if (e.key === 'Enter') doLogin(); });

          async function doLogin() {
            const pwd = document.getElementById('pwd').value.trim();
            const err = document.getElementById('login-err');
            if (!pwd) { err.textContent = '请输入密码'; return; }
            try {
              const tree = await (await fetch('./file-tree.json')).json();
              const first = findFirstFile(tree);
              if (!first) { bootApp(pwd); return; }
              const enc = await (await fetch('./encrypted/' + first)).text();
              const dec = CryptoJS.AES.decrypt(enc, pwd).toString(CryptoJS.enc.Utf8);
              if (dec) { err.textContent = ''; bootApp(pwd); }
              else     { err.textContent = '密码错误，请重试'; }
            } catch(e) { err.textContent = '验证失败，请重试'; }
          }

          function findFirstFile(nodes) {
            for (const n of nodes) {
              if (n.type === 'file') return n.path;
              if (n.type === 'dir') { const f = findFirstFile(n.children); if (f) return f; }
            }
            return null;
          }

          async function bootApp(pwd) {
            PASSWORD = pwd;
            document.getElementById('login').style.display = 'none';
            document.getElementById('app').style.display   = 'flex';
            const tree = await (await fetch('./file-tree.json')).json();
            renderTree(tree, document.getElementById('tree'));
          }

          function renderTree(nodes, container) {
            nodes.forEach(node => {
              if (node.type === 'dir') {
                const folder = document.createElement('div');
                folder.className = 'tree-folder';
                const row = document.createElement('div');
                row.className = 'tree-item';
                row.innerHTML = `<span class="arrow">▶</span><span class="icon">📂</span><span class="label">${node.name}</span>`;
                row.onclick = () => folder.classList.toggle('open');
                const children = document.createElement('div');
                children.className = 'tree-children';
                renderTree(node.children, children);
                folder.appendChild(row);
                folder.appendChild(children);
                container.appendChild(folder);
              } else {
                const item = document.createElement('div');
                item.className = 'tree-item';
                item.innerHTML = `<span class="icon">${getIcon(node.name)}</span><span class="label" title="${node.name}">${node.name}</span>`;
                item.dataset.path = node.path;
                item.dataset.name = node.name;
                item.onclick = () => openFile(node.path, node.name, item);
                container.appendChild(item);
              }
            });
          }

          function getIcon(name) {
            const ext = name.split('.').pop().toLowerCase();
            return { html:'🌐', htm:'🌐', md:'📝', txt:'📄', js:'📜', css:'🎨', json:'📋',
                     png:'🖼️', jpg:'🖼️', jpeg:'🖼️', gif:'🖼️', svg:'🖼️', pdf:'📕' }[ext] || '📄';
          }

          let activeItem = null;
          async function openFile(encPath, name, itemEl) {
            if (activeItem) activeItem.classList.remove('active');
            activeItem = itemEl;
            itemEl.classList.add('active');
            document.getElementById('topbar').textContent = name;
            document.getElementById('placeholder').style.display  = 'none';
            document.getElementById('file-content').style.display = 'none';
            document.getElementById('loading').style.display      = 'block';
            try {
              const enc = await (await fetch('./encrypted/' + encPath)).text();
              const dec = CryptoJS.AES.decrypt(enc, PASSWORD).toString(CryptoJS.enc.Utf8);
              document.getElementById('loading').style.display      = 'none';
              document.getElementById('file-content').style.display = 'block';
              document.getElementById('file-title').textContent     = name;
              const body = document.getElementById('file-body');
              const ext  = name.split('.').pop().toLowerCase();
              if (['html','htm'].includes(ext)) {
                const url = URL.createObjectURL(new Blob([dec], { type: 'text/html' }));
                body.innerHTML = `<iframe src="${url}"></iframe>`;
              } else if (ext === 'md') {
                body.innerHTML = renderMd(dec);
              } else {
                body.innerHTML = `<pre>${escHtml(dec)}</pre>`;
              }
            } catch(e) {
              document.getElementById('loading').style.display = 'none';
              document.getElementById('file-content').style.display = 'block';
              document.getElementById('file-body').innerHTML = '<pre style="color:#f77">解密失败</pre>';
            }
          }

          function escHtml(s) {
            return s.replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;');
          }

          function renderMd(src) {
            let h = escHtml(src);
            h = h.replace(/^### (.+)$/gm, '<h3>$1</h3>');
            h = h.replace(/^## (.+)$/gm,  '<h2>$1</h2>');
            h = h.replace(/^# (.+)$/gm,   '<h1>$1</h1>');
            h = h.replace(/\*\*(.+?)\*\*/g, '<strong>$1</strong>');
            h = h.replace(/\*(.+?)\*/g,     '<em>$1</em>');
            h = h.replace(/^&gt; (.+)$/gm, '<blockquote>$1</blockquote>');
            h = h.replace(/^[-*] (.+)$/gm, '<li>$1</li>');
            h = h.replace(/^---$/gm, '<hr>');
            h = h.replace(/\n/g, '<br>');
            return `<div class="md-body">${h}</div>`;
          }

          window.openFileByName = function(filename) {
            document.querySelectorAll('.tree-item').forEach(item => {
              if (item.dataset.name === filename) item.click();
            });
          };
        </script>
        </body>
        </html>
        EOF

    - name: Setup Pages
      uses: actions/configure-pages@v4

    - name: Upload artifact
      uses: actions/upload-pages-artifact@v3
      with:
        path: '.'

    - name: Deploy to GitHub Pages
      id: deployment
      uses: actions/deploy-pages@v4
